networks:
  magnit:
    external: true

services:
  app:
    image: ${APP_IMAGE:-ghcr.io/nagaimatsuge/magnit-pos:latest}
    container_name: pos-frontend
    restart: unless-stopped
    networks:
      - magnit
